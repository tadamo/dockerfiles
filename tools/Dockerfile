FROM alpine:latest
RUN apk update && \
    apk add --no-cache \
    bash \
    ca-certificates \
    openssl \
    curl \
    httpie \
    bind-tools \
    netcat-openbsd \
    jq \
    coreutils \
    openldap-clients
