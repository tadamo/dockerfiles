#!/usr/bin/env bash
LOG_INTERVAL_SECONDS=${LOG_INTERVAL_SECONDS:-1}

# Trap HANGUP signal and exit with success
trap "exit 0" HUP

# Wait forever, send date/time stamp to STDOUT at the given interval
while true; do
    date
    sleep $LOG_INTERVAL_SECONDS
done
